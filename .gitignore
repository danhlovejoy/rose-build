# Credentials
.env

# Build output (generated, not source)
build/output/
build/aiml2003/
build/aiml2013/

# Standalone build temp files (should be cleaned up, but just in case)
*.standalone.tmp

# Scratch/prep work
prep-scratch/

# Quiz content (answers in plaintext)
quizzes/
*.imscc

# Final exam content (answers in plaintext)
final-exam/

# Copyright sources can't be uploaded
readings/szeliski/

# OS files
.DS_Store
.venv/
__pycache__/

# Office lock files (created when a doc is open)
~$*

# Claude Code agent worktrees and local config
.claude/
